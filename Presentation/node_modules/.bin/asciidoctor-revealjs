#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@asciidoctor/reveal.js/npm/asciidoctor-revealjs" "$@"
  ret=$?
else 
  node  "$basedir/../@asciidoctor/reveal.js/npm/asciidoctor-revealjs" "$@"
  ret=$?
fi
exit $ret
