/// <reference types="react" />
import { Chart } from 'chart.js';
import type { ChartOptions, ChartData, ChartType, Plugin, UpdateMode } from 'chart.js';
export interface ChartProps {
    data: ChartData;
    options: ChartOptions;
    type: ChartType;
    plugins?: Plugin[];
    updateMode?: UpdateMode;
    id?: string;
    height?: number;
    width?: number;
}
export declare const ReactChart: {
    ({ id, data, options, type, plugins, updateMode, height, width, }: ChartProps): JSX.Element;
    register: typeof Chart.register;
};
