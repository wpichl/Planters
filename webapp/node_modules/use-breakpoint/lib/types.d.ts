export declare type Config = {
    readonly [key: string]: number;
};
export declare type Breakpoint<C extends Config> = {
    breakpoint: keyof C;
    maxWidth?: number;
    minWidth: C[keyof C];
};
export declare type MediaQuery<C extends Config> = {
    breakpoint: keyof C;
    maxWidth?: number;
    minWidth: C[keyof C];
    query: string;
};
